package aitu.network.aitunetwork.model.enums;
// TODO: consider reusing PostType with IssuerType
//  since they have the same content if there wouldn't be difference
public enum IssuerType {
    GROUP,
    USER
}
